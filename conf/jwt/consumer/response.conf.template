# JWT Consumer configuration

# valid JWT variables
VALID_ALGORITHM="ES256"
VALID_TYP="JWT"
VALID_DLT="namecoin"
VALID_DSN="ipfs"

# authorization based on social graph
AUTHORIZATION="true"
PROFILE_URI="/ipns/<hash>/name.rdf"

# site cache
TOKEN_CACHE="$TMP_DIR/token_cache.dat"
PROFILE_URI_CACHE="$TMP_DIR/profile_uri"
